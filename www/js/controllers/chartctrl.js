app
	.controller('ChartCtrl', function($scope) {
		var width = window.height,
			height = window.width,
			chart = d3.select("#chart-div").append("svg")
						.attr("width", )
						.attr("height", );
	});